import {IFormConfig} from '@widgets/form/types/IFormConfig';

export const form__selectProject :IFormConfig= {
  title:  'Select Project Form',
  formId: 'select-project-form',
  items:  [{name: 'project', title: 'Project', type: 'project'}],
};
