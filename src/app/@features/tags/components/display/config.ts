import {IFormConfig} from '@widgets/form/types/IFormConfig';

export const form__selectTags: IFormConfig = {
  title:  'Select Tags',
  formId: 'select-tags-form',
  items:  [{name: 'tags', title: 'Tags', type: 'tags'}],
}